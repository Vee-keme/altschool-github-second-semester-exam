import React from "react";

//return repo details info after the repo is clicked in reposcard
function RepoData({ currentItems }) {
  return (
    <div>
      <h1>RepoData</h1>
    </div>
  );
}
export default RepoData;
